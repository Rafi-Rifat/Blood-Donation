class postData{
  final String Uid;
  final String name;
  final String date;
  final String postText;
  final String image;
  final String userId;
  // final bool IsOnline = false;
  // final DonerLatLang;

  const postData(this.Uid, this.name, this.date, this.postText, this.image, this.userId);
}