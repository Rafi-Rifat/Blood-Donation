class postData{
  final String Uid;
  final String name;
  final String date;
  final String postText;
  // final bool IsOnline = false;
  // final DonerLatLang;

  const postData(this.Uid, this.name, this.date, this.postText);
}